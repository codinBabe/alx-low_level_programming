#include <stdio.h>

/**
 * main - Entry point declare int to print number in base 10 from 0
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
