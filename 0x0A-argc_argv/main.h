#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
int main(int argc, char *argv[]);
int main(int argc, char __attribute__((unused))*argv[]);
int _putchar(char c);
#endif
